_backups/
.claude/
# Local-only audit enrichment (full findings + mirrored reports). NEVER commit:
# the pages deploy is PUBLIC and reports detail unfixed security findings.
local/
.wrangler/
repo-workflows/
__pycache__/
*.pyc

# Transient Strategy→Cowork export (merged into the workspace ledger, not committed here)
strategy_decisions.json
strategy_cowork_brief.md
.tmp_ledgertest*/
